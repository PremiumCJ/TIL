groupby() 함수를 사용하지만, 그 과정이 단계적으로 따로 하는 것이 아니라, 아래와 같이 한 번의 라인으로 수행가능하다. 
앞서 mean() 을 매소드로 사용한 것 부분을 transform() 으로 바꾸고, 거기에 인자로 mean을 넣어주면 된다!

df["User Mean) = df.groupby('User ID)["Purchase"].transform('mean')
